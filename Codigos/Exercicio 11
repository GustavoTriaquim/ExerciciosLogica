programa {
  funcao inicio() {
    inteiro numero, i, resultado

    escreva("Digite um numero: ")
    leia(numero)

    para (i = 0; i <= 10; i++) {
      resultado = numero * i

      escreva(numero, " x ", i, " = ", resultado, " \n")
    }
  }
}
